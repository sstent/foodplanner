services:
  foodtracker:
    build: .
    ports:
      - "8999:8999"
    environment:
      - DATABASE_URL=sqlite:////app/data/meal_planner.db
    volumes:
      - ./alembic:/app/alembic
      - ./data:/app/data